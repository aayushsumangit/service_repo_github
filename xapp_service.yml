apiVersion: v1
kind: Service
metadata:
  name: my-service
  namespace: default
spec:
  selector:
    app: my-app
  ports:
    - protocol: TCP
      port: 8090      # Service port
      targetPort: 8080 # Pod port
      nodePort: 30001 # NodePort (explicit port number)
  type: NodePort
